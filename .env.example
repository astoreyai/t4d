# T4DM Environment Configuration
# Copy to .env and set values before starting
#
# Quick setup: Run ./scripts/setup-env.sh to auto-generate secure passwords

# ============================================
# T4DM Application
# ============================================
T4DM_SESSION_ID=default

# ============================================
# T4DX Storage Engine
# ============================================
# T4DM_T4DX_DATA_DIR=.data/t4dx  # Default data directory

# ============================================
# Embedding Configuration (BGE-M3)
# ============================================
T4DM_EMBEDDING_MODEL=BAAI/bge-m3
T4DM_EMBEDDING_DIMENSION=1024
T4DM_EMBEDDING_DEVICE=cuda:0
T4DM_EMBEDDING_USE_FP16=true
T4DM_EMBEDDING_BATCH_SIZE=32
T4DM_EMBEDDING_MAX_LENGTH=512
T4DM_EMBEDDING_CACHE_DIR=./models

# ============================================
# Memory Parameters
# ============================================
T4DM_FSRS_DEFAULT_STABILITY=1.0
T4DM_FSRS_RETENTION_TARGET=0.9
T4DM_HEBBIAN_LEARNING_RATE=0.1
T4DM_HEBBIAN_INITIAL_WEIGHT=0.1

# ============================================
# Consolidation Settings
# ============================================
T4DM_CONSOLIDATION_MIN_SIMILARITY=0.75
T4DM_CONSOLIDATION_MIN_OCCURRENCES=3
T4DM_CONSOLIDATION_SKILL_SIMILARITY=0.85

# ============================================
# Retrieval Weights
# ============================================
T4DM_RETRIEVAL_SEMANTIC_WEIGHT=0.4
T4DM_RETRIEVAL_RECENCY_WEIGHT=0.25
T4DM_RETRIEVAL_OUTCOME_WEIGHT=0.2
T4DM_RETRIEVAL_IMPORTANCE_WEIGHT=0.15

# ============================================
# ACT-R Parameters
# ============================================
T4DM_ACTR_DECAY=0.5
T4DM_ACTR_THRESHOLD=0.0
T4DM_ACTR_NOISE=0.5

# ============================================
# Environment (affects security validation)
# ============================================
# T4DM_ENVIRONMENT=development  # or: staging, production

# ============================================
# Observability (Optional)
# ============================================
# T4DM_OTEL_ENABLED=false
# T4DM_OTEL_ENDPOINT=http://localhost:4317
# T4DM_OTEL_INSECURE=true  # Set false in production

# ============================================
# REST API Configuration
# ============================================
T4DM_API_HOST=0.0.0.0
T4DM_API_PORT=8765
T4DM_API_WORKERS=4
# T4DM_API_CORS_ORIGINS=*  # Restrict in production

# ============================================
# Entity Extraction (Optional)
# ============================================
T4DM_AUTO_EXTRACTION_ENABLED=true
T4DM_EXTRACTION_CONFIDENCE_THRESHOLD=0.7
T4DM_EXTRACTION_USE_LLM=false  # Requires OpenAI API key
# T4DM_EXTRACTION_LLM_MODEL=gpt-4o-mini

# ============================================
# Test Mode (for development/CI)
# ============================================
# T4DM_TEST_MODE=false  # Set true to bypass password validation
